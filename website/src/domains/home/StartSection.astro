---
import { button } from "@/ui/Button";
import { Image } from "astro:assets";
import blackWaveBackground from "@/assets/images/logo-background.svg";
---

<script>
  import { animate, scroll } from "motion";

  const fadeOutUp = (selector: string) => {
    const element = document.querySelector(selector);
    if (element) {
      scroll(animate(element, { y: [0, -200], opacity: [1, 0] }), {
        target: element,
        offset: ["0 0.1", "0.5 0"],
      });
    }
  };
  fadeOutUp("#hero-body");

  const heroBackground = document.querySelector("#hero-background");
  if (heroBackground) {
    scroll(animate(heroBackground, { x: [0, 1000], scale: [1, 2] }), {
      target: heroBackground,
      offset: ["0 0", "1 0"],
    });
  }
</script>
<div class="absolute h-[100vh] -mt-header w-full overflow-hidden">
  <Image
    id="hero-background"
    class="w-full h-full object-cover overflow-hidden"
    src={blackWaveBackground}
    alt="black wave background"
  />
</div>

<section
  id="company-marquee-section"
  class="relative flex items-center pb-40 px-md-20 pt-10 min-h-[calc(100vh-theme('spacing.header'))] overflow-x-clip"
>
  <div aria-hidden="true" class="-z-0 flex gap-12 absolute left-0 h-[300%] transform rotate-30 -mt-20">
    <div class="w-[1px] h-full bg-neutral-800"></div>
    <div class="w-[1px] h-full bg-neutral-800"></div>
  </div>
  <div class="container z-10 mx-auto h-full gap-24 flex-grow items-center">
    <div id="hero-body" class="flex flex-col items-center justify-center text-center px-2">
      <!-- <BackPattern color="secondary" className="mx-auto max-w-prose">
        <div
          class="bg-black border border-solid border-secondary-400 text-white p-4 flex flex-col gap-8 items-start"
        >
          <p class="text-left">
            Wir suchen unsere ersten Freelancer-Projekte. Sichern Sie sich <TextHighlight>
              konstengünstige
            </TextHighlight>
            Software-Entwicklung ohne Qualitätsabstriche.
          </p>
          <a href="#kontakt" class={button({ size: "sm", variant: "secondary" })}>Experten anheuern</a>
        </div>
      </BackPattern> -->

      <h1 id="hero-title" class="font-sans text-7xl text-primary-200 mt-4">
        <span
          class="bg-gradient-to-br from-primary-200 via-primary-200 to-primary-500 text-transparent bg-clip-text"
          >Drei Köpfe.</span
        >
        <span class="block md:inline">
          Ein <span
            class="bg-gradient-to-r from-primary-200 via-primary-500 to-primary-100 text-transparent bg-clip-text animate-pulse"
          >
            Puls
          </span>.</span
        >
      </h1>

      <p id="hero-description" class="text-xl max-w-[800px] pt-8">
        Verwandt im echten Leben, eingespielt im Projekt – drei Entwickler, die Klartext sprechen und echten
        Mehrwert schaffen.
      </p>
      <div id="hero-button" class="mt-14">
        <a
          href="#was-wir-bewirken"
          class={button({
            size: "lg",
            class: "text-lg",
            increaseHitbox: true,
          })}>Was wir für dich erreichen</a
        >
      </div>
      <div class="flex flex-wrap items-center justify-center gap-4 mt-14">
        <div
          class="border border-solid border-neutral-800 bg-black/80 backdrop-blur-lg rounded-full px-4 py-2 text-primary-100"
        >
          Pragmatisch
        </div>
        <div
          class="border border-solid border-neutral-800 bg-black/80 backdrop-blur-lg rounded-full px-4 py-2 text-primary-100"
        >
          Qualitätsbewusst
        </div>
        <div
          class="border border-solid border-neutral-800 bg-black/80 backdrop-blur-lg rounded-full px-4 py-2 text-primary-100"
        >
          Kommunikativ
        </div>
        <div
          class="border border-solid border-neutral-800 bg-black/80 backdrop-blur-lg rounded-full px-4 py-2 text-primary-100"
        >
          Verlässlich
        </div>
      </div>
    </div>
  </div>
</section>
